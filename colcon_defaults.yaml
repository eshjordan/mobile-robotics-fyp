{
  "build": {
    "symlink-install": true,
    "executor": "parallel",
    "mixin": [
      "rel-with-deb-info",
      # "debug",
      "build-testing-on",
      "ccache",
      "compile-commands",
      "enable-linting",
    ],
    "packages-skip-regex": [
    ],
  },
  "test": {
    "packages-skip-regex": [
    ],
  },
  "test-result": {
    "all": true,
    "verbose": true
  }
}
